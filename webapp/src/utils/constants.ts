export const SE_SOCKET_EVENT = 'SESocketEvent'

export enum OSC_EVENTS {
  SE_CONNECTED = 'SESocket::connected',
  SE_CONNECTING = 'SESocket::connecting',
  SE_DISCONNECTED = 'SESocket::disconnected',
  SE_AUTHORIZED = 'SESocket::authorized',
  SE_UNAUTHORIZED = 'SESocket::unauthorized',
  SE_EVENT = 'SESocket::event'
}
